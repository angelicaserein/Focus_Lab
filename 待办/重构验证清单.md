# 重构验证清单

> 这次重构（拆分 Focus/History 巨型组件、拆分 Context、抽取共享工具、整理 CSS）已完成，
> `npm run build` 编译通过。以下为**需要在浏览器里手动核对**的功能项 —— 项目无自动化测试，逐项点一遍确认行为没变样。
>
> 用法：`npm run dev` 启动后打开浏览器，照着勾。发现问题就记在对应项下面。

---

## 1. Todo（任务列表）

- [ ] 添加任务：输入框回车 / 点「添加」都能新增，新项有淡入动画
- [ ] 删除任务：右下角弹出「已删除…」toast，5 秒内点「撤销」能恢复到原位置
- [ ] **撤销恢复联动**：若被删的任务原本已勾选进专注，撤销后它**重新回到专注选中状态**
- [ ] 编辑任务：点铅笔 ✎ 进入编辑，回车保存 / Esc 取消 / 失焦保存
- [ ] 勾选完成：勾选框变色、文字加删除线
- [ ] 筛选标签：全部 / 未完成 / 已完成 三个 tab 过滤正确
- [ ] 底部统计行「已完成 X / 总计 Y」数字正确

## 2. Focus 普通视图

- [ ] 在下方任务列表整行点击，可多选任务进入专注集合（高亮）
- [ ] 上方「已选任务」显示对应 chips 和数量徽标
- [ ] chip 上的 × 能单独移除某个任务
- [ ] 「清除」按钮清空所有已选
- [ ] 「重置」按钮在未计时（秒数为 0）时为禁用态
- [ ] 点「▶ 开始专注」进入沉浸视图，计时开始走动

## 3. Focus 沉浸视图

- [ ] 3D 模型正常加载，并能用鼠标拖动旋转（在自身中心原地转，不乱飘）
- [ ] 烧瓶液面随专注时间上升
- [ ] 「暂停 / 继续」切换计时；状态点和文字（专注中 / 已暂停）同步
- [ ] 「重置」把秒数归零（停在沉浸视图内）
- [ ] 单任务「完成 ✓」：标记任务完成、移出专注、记一条 completed 记录
- [ ] 单任务「移除 ×」：移出专注、记一条 removed 记录
- [ ] 「结束专注」：把剩余任务以 ended 结算、清空并退出沉浸视图
- [ ] （仅开发环境）右下角「调试」面板：液体进度滑块、卡片显隐、模型动画开关都生效

## 4. History（历史）

- [ ] 顶部统计：累计次数 / 累计时长 / 今日时长 / 最长单次 / 平均时长 数字正确
- [ ] 近 7 天柱状图高度、日期标签正确（今天在最右）
- [ ] 任务专注排行（前 6）排序、时长、次数正确
- [ ] 记录按「天」分组，组内再按「一次专注」(sessionId) 归组
- [ ] 多任务的一次专注显示成一张卡，逐任务带「完成/移除/结束」徽章
- [ ] 「清除记录」需点两次确认（先变「确认清除？」）

## 5. 持久化

- [ ] 刷新页面后，todos 和专注记录都还在（localStorage：`todos_v1` / `focus_records_v1`）

## 6. 构建

- [x] `npm run build` 通过，无新增报错/告警（仅有原本就存在的 three.js 大文件提示）

---

## ⏳ 待定决策（还没改，等想好再统一处理）

**「重置」按钮要不要清掉 sessionId？**

- 现状（保持原行为）：点重置只清「开始时间」，**保留 sessionId**。
  → 好处：重置后继续专注，History 里这些任务仍归在**同一张「一次专注」卡**里。
- 备选（计划里原本想改的）：重置时连 sessionId 也清掉 = **重置后视为开一次全新专注**。
  → 影响：重置后完成的任务会在 History 里**散成独立卡片**，不再和之前的归在一起。

决定怎么处理后告诉我，我再改 `src/hooks/useFocusTimer.js` 的 `resetTimer()`。

- [ ] 已决定：____________
