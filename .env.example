# ════════════════════════════════════════════════════════════
#  环境变量模板（这是模板，复制成 .env 再填）
#
#  怎么用：
#    1. 把本文件复制一份，改名为  .env  （和 package.json 同一层）
#    2. 把下面的钥匙填进引号后面的等号右边
#    3. 重启  npm run dev  才会生效
#
#  注意：真正的 .env 已被 git 忽略，不会上传，放钥匙是安全的。
# ════════════════════════════════════════════════════════════

# AI 对话用的 OpenAI API 钥匙（sk-proj-... 或 sk-...）。
#   - 留空 / 不填  → 专注页 AI 对话自动使用「示例对话」
#   - 填上钥匙      → 走真正的 AI 分支（接入代码见 src/utils/ai/aiChat.js）
VITE_OPENAI_API_KEY=
